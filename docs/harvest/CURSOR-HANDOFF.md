# Cursor handoff

Read [APP-MAP.md](APP-MAP.md), [BACKEND-SCOUT.md](BACKEND-SCOUT.md), and [app-map.json](app-map.json). Raw evidence remains in the local Harvest workspace. Inspect existing repository instructions before implementation.

Prototype content hash: `ba4f63703e77baa7c21182badef5dad1f5789ba4b5e962a38cc25d5c49dc972c`. Visual approval: not granted.

Use the application map to connect each implemented backend capability to its route, components, and controls. Preserve the distinction between observed reference behavior, prototype simulations, and proposed product behavior. Replace fixtures through explicit data/service boundaries. Resolve material unknown product and permission rules before dependent implementation. Verify loading, success, failure, authorization, and persistence against the agreed contract. Update the map with actual implementation locations and checks as work lands.

No backend provisioning or Eden API integration is authorized by this handoff. Service proposals are vendor-neutral. Captured coverage is partial; unsupported canvas/frame/account surfaces and all unresolved controls remain in the structured map. Compilation and semantic confidence are not human visual acceptance.

Launch metadata: `["npm", "run", "dev", "--", "--port", "5181"]` in the generated project. Deterministic scenario reset: `window.__harvestResetScenario()` when implemented. Verification and measured mismatches are in Harvest's independent report.

Deterministic documentation validation: 0 errors. See app-map.json for individual references and binding checks.
